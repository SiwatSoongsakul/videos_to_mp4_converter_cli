[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mp4-videos-converter"
version = "1.0"
dependencies = [
    "typer",
]

[project.scripts]
v2mp4 = "app.converter:app"